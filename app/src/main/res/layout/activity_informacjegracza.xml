<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/imieTextView"
        android:layout_width="161dp"
        android:layout_height="75dp"
        android:text="Imię"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/nickTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.82" />

    <TextView
        android:id="@+id/nickTextView"
        android:layout_width="161dp"
        android:layout_height="98dp"
        android:layout_marginBottom="16dp"
        android:text="Nick"
        android:textSize="16sp"
        app:layout_constraintBottom_toTopOf="@+id/postacieTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/postacieTextView"
        android:layout_width="161dp"
        android:layout_height="348dp"
        android:layout_below="@id/nickTextView"
        android:layout_marginBottom="16dp"
        android:maxLines="10"
        android:text="Postacie"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
